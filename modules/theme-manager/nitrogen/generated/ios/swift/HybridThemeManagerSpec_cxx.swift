///
/// HybridThemeManagerSpec_cxx.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

import Foundation
import NitroModules

/**
 * A class implementation that bridges HybridThemeManagerSpec over to C++.
 * In C++, we cannot use Swift protocols - so we need to wrap it in a class to make it strongly defined.
 *
 * Also, some Swift types need to be bridged with special handling:
 * - Enums need to be wrapped in Structs, otherwise they cannot be accessed bi-directionally (Swift bug: https://github.com/swiftlang/swift/issues/75330)
 * - Other HybridObjects need to be wrapped/unwrapped from the Swift TCxx wrapper
 * - Throwing methods need to be wrapped with a Result<T, Error> type, as exceptions cannot be propagated to C++
 */
open class HybridThemeManagerSpec_cxx {
  /**
   * The Swift <> C++ bridge's namespace (`margelo::nitro::thememanager::bridge::swift`)
   * from `ThemeManager-Swift-Cxx-Bridge.hpp`.
   * This contains specialized C++ templates, and C++ helper functions that can be accessed from Swift.
   */
  public typealias bridge = margelo.nitro.thememanager.bridge.swift

  /**
   * Holds an instance of the `HybridThemeManagerSpec` Swift protocol.
   */
  private var __implementation: any HybridThemeManagerSpec

  /**
   * Holds a weak pointer to the C++ class that wraps the Swift class.
   */
  private var __cxxPart: bridge.std__weak_ptr_HybridThemeManagerSpec_

  /**
   * Create a new `HybridThemeManagerSpec_cxx` that wraps the given `HybridThemeManagerSpec`.
   * All properties and methods bridge to C++ types.
   */
  public init(_ implementation: any HybridThemeManagerSpec) {
    self.__implementation = implementation
    self.__cxxPart = .init()
    /* no base class */
  }

  /**
   * Get the actual `HybridThemeManagerSpec` instance this class wraps.
   */
  @inline(__always)
  public func getHybridThemeManagerSpec() -> any HybridThemeManagerSpec {
    return __implementation
  }

  /**
   * Casts this instance to a retained unsafe raw pointer.
   * This acquires one additional strong reference on the object!
   */
  public func toUnsafe() -> UnsafeMutableRawPointer {
    return Unmanaged.passRetained(self).toOpaque()
  }

  /**
   * Casts an unsafe pointer to a `HybridThemeManagerSpec_cxx`.
   * The pointer has to be a retained opaque `Unmanaged<HybridThemeManagerSpec_cxx>`.
   * This removes one strong reference from the object!
   */
  public class func fromUnsafe(_ pointer: UnsafeMutableRawPointer) -> HybridThemeManagerSpec_cxx {
    return Unmanaged<HybridThemeManagerSpec_cxx>.fromOpaque(pointer).takeRetainedValue()
  }

  /**
   * Gets (or creates) the C++ part of this Hybrid Object.
   * The C++ part is a `std::shared_ptr<HybridThemeManagerSpec>`.
   */
  public func getCxxPart() -> bridge.std__shared_ptr_HybridThemeManagerSpec_ {
    let cachedCxxPart = self.__cxxPart.lock()
    if cachedCxxPart.__convertToBool() {
      return cachedCxxPart
    } else {
      let newCxxPart = bridge.create_std__shared_ptr_HybridThemeManagerSpec_(self.toUnsafe())
      __cxxPart = bridge.weakify_std__shared_ptr_HybridThemeManagerSpec_(newCxxPart)
      return newCxxPart
    }
  }

  

  /**
   * Get the memory size of the Swift class (plus size of any other allocations)
   * so the JS VM can properly track it and garbage-collect the JS object if needed.
   */
  @inline(__always)
  public var memorySize: Int {
    return MemoryHelper.getSizeOf(self.__implementation) + self.__implementation.memorySize
  }

  /**
   * Call dispose() on the Swift class.
   * This _may_ be called manually from JS.
   */
  @inline(__always)
  public func dispose() {
    self.__implementation.dispose()
  }

  // Properties
  public final var theme: Int32 {
    @inline(__always)
    get {
      return self.__implementation.theme.rawValue
    }
  }
  
  public final var palette: Int32 {
    @inline(__always)
    get {
      return self.__implementation.palette.rawValue
    }
  }
  
  public final var fullTheme: Int32 {
    @inline(__always)
    get {
      return self.__implementation.fullTheme.rawValue
    }
  }

  // Methods
  @inline(__always)
  public final func setTheme(theme: Int32) -> bridge.Result_void_ {
    do {
      try self.__implementation.setTheme(theme: margelo.nitro.thememanager.FullTheme(rawValue: theme)!)
      return bridge.create_Result_void_()
    } catch (let __error) {
      let __exceptionPtr = __error.toCpp()
      return bridge.create_Result_void_(__exceptionPtr)
    }
  }
  
  @inline(__always)
  public final func setPalette(palette: Int32) -> bridge.Result_void_ {
    do {
      try self.__implementation.setPalette(palette: margelo.nitro.thememanager.Palette(rawValue: palette)!)
      return bridge.create_Result_void_()
    } catch (let __error) {
      let __exceptionPtr = __error.toCpp()
      return bridge.create_Result_void_(__exceptionPtr)
    }
  }
  
  @inline(__always)
  public final func listen(callback: bridge.Func_void_ThemeManagerPayload) -> bridge.Result_void_ {
    do {
      try self.__implementation.listen(callback: { () -> (ThemeManagerPayload) -> Void in
        let __wrappedFunction = bridge.wrap_Func_void_ThemeManagerPayload(callback)
        return { (__payload: ThemeManagerPayload) -> Void in
          __wrappedFunction.call(__payload)
        }
      }())
      return bridge.create_Result_void_()
    } catch (let __error) {
      let __exceptionPtr = __error.toCpp()
      return bridge.create_Result_void_(__exceptionPtr)
    }
  }
}
